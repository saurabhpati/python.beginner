# The dir function returns a sorted list of strings definer by the module.

# This will print the names of the members exposed by sys module.
import sys;
print(dir(sys));

import ChemicalFactory;
ChemicalFactory.Cement();
ChemicalFactory.Paint();
ChemicalFactory.Resin();